# 🤖 Rapport Audit Maintenance — HELP Confort

**Date** : 2026-05-12
**Agent** : Claude (Cowork Maintenance Mode)
**Périmètre** : Site public (33 HTML) + Back-Office admin-pro (24 HTML) + 14 Edge Functions Supabase

---

## 📊 Résumé exécutif

| Sévérité | Nombre | Action |
|---|---|---|
| 🔴 Critique | 7 | À corriger sous 7j |
| 🟡 Important | 9 | À corriger sous 30j |
| 🟢 Optimisation | 6 | Backlog |

**Score santé global** : 72/100 (jaune — stable mais perfectible)

Points forts : structure SEO solide (Schema LocalBusiness sur 7 pages, sitemap 48 URLs, schema.org bien rempli), lazy-loading 100% sur index, charte couleurs respectée (260 occurrences), .gitignore propre, Edge Functions ont try/catch + CORS systématique, pas de secret hardcodé.

---

## 🔴 CRITIQUE — À corriger sous 7 jours

### C1. Tracking analytics inactif sur l'ensemble du site
**Constat** : 111 occurrences de `GTM-XXXXXXX` et 80 de `G-XXXXXXXXXX` réparties sur ~30 pages. Les scripts sont protégés par un garde `if(id==='G-XXXXXXXXXX')return;` donc ne crashent pas, mais aucune donnée n'est collectée.
**Impact** : zéro mesure de conversion, ROI invisible, pas d'optimisation possible.
**Fix** :
```bash
cd "/Users/HP/Documents/Claude/Projects/SITE INTERNET"
# Remplacer par les vrais IDs (à demander à Florian)
grep -rl "G-XXXXXXXXXX" --include="*.html" . | xargs sed -i '' 's/G-XXXXXXXXXX/G-REELID/g'
grep -rl "GTM-XXXXXXX" --include="*.html" . | xargs sed -i '' 's/GTM-XXXXXXX/GTM-REELID/g'
```

### C2. Numéro de téléphone parasite `0321000000`
**Constat** : `href="tel:0321000000"` détecté dans le repo (probablement bouton placeholder ou ancien). Le vrai numéro est `+33366100134`.
**Impact** : risque qu'un visiteur compose un faux numéro.
**Fix** : `grep -rn "0321000000" --include="*.html" .` puis remplacer ou supprimer le bouton.

### C3. Cache busting incohérent sur styles.css (4 versions en circulation)
**Constat** : 30 pages utilisent `?v=1778436727`, mais `index.html` (`?v=1778437762`), `zones-intervention.html` (`?v=1778438047`) et `plombier-saint-omer.html` (`?v=1778499476`) ont des versions différentes.
**Impact** : navigateur peut servir un CSS daté sur certaines pages → rendu cassé pour les visiteurs entre 2 déploiements.
**Fix** : unifier sur la version la plus récente (`1778499476`) :
```bash
grep -rl 'styles\.css?v=' --include="*.html" . | xargs sed -i '' 's/styles\.css?v=[0-9]*/styles.css?v=1778499476/g'
```

### C4. Idem pour `hc-widgets.js` (3 versions)
**Constat** : 29 pages en `?v=1778322703`, 2 en `?v=1778338098`, 1 en `?v=1778351200`.
**Fix** : unifier sur `1778351200`.

### C5. Vidéos lourdes dupliquées hors gitignore théorique
**Constat** : `videos/hero-metier-OLD.mp4` (130MB) et `videos/hero-metier.mp4.original` (130MB) présents sur disque. Le `.gitignore` les exclut (`*.original`, `*-OLD.*`) — vérifier qu'ils ne sont PAS poussés. `hero-metier-NOAUDIO-bak.mp4` (14MB) idem (`*-bak.*`).
**Impact** : si commités, repo gonflé, push Netlify long, dépassement quota possible.
**Fix** : `git ls-files | grep -E '\.original|OLD\.mp4|-bak\.'` puis `git rm --cached` si trouvé.

### C6. Emails placeholder dans le code public
**Constat** : `vous@entreprise.fr` et `vous@exemple.fr` présents dans des pages publiques.
**Impact** : exposition de placeholders, mauvais signal qualité.
**Fix** : `grep -rn "vous@entreprise.fr\|vous@exemple.fr" --include="*.html" .` puis supprimer/remplacer.

### C7. `console.log` en production (10 fichiers)
**Constat** : présent dans `index.html`, `realisations.html`, `admin-pro/index.html`, `admin-pro/calendar.html`, `admin-pro/medias.html`, `admin-pro/realisations.html`, `admin-pro/settings.html`, `admin-pro/wizard-google.html`, `admin-pro/wizard-ga4.html`, `admin-pro/assets/layout.js`, plus les 4 fichiers `assets/hc-*.js`.
**Impact** : bruit DevTools, possibles fuites d'info en console (objets utilisateur, tokens partiels).
**Fix** : remplacer par un wrapper `HCLog` désactivable en prod, ou ajouter une étape de build qui les strip.

---

## 🟡 IMPORTANT — À corriger sous 30 jours

### I1. Meta descriptions hors plage SEO (50–160 caractères)
**Constat** : `index.html` (222c), `depannage-saint-omer.html` (222c), `contact.html` (183c), `plombier-saint-omer.html` (183c) — toutes au-dessus du seuil de troncature Google (~155–160c).
**Fix** : réécrire en 145–155 caractères max.

### I2. 15 titles > 70 caractères (tronqués en SERP)
**Constat** : `depannage-saint-martin-lez-tatinghem.html` à 96c, `actualites.html` 83c, `contrats-entretien.html` 83c, etc.
**Fix** : raccourcir à 60–65 caractères. Exemple : `Dépannage Saint-Martin-lez-Tatinghem (62500) — Plomberie | HELP Confort` (74c → encore long).

### I3. `mentions-legales.html` : pas de balise canonical
**Fix** : ajouter `<link rel="canonical" href="https://www.depan59-62.fr/mentions-legales.html">`.

### I4. `index.html` trop lourd (252 KB / 5825 lignes / 28 blocs `<script>` et `<style>` inline)
**Constat** : 39 media queries inline + énormément de CSS dupliqué de `styles.css`.
**Impact** : TTI (Time To Interactive) impacté sur mobile 3G, surcoût bande passante.
**Fix** : externaliser le CSS critique dans une feuille dédiée + lazy-load le reste. Le script `scripts/inline-critical-css.py` existe déjà → vérifier qu'il tourne en sens inverse.

### I5. Manifest PWA incomplet
**Constat** : `manifest.json` n'a pas d'icône PNG en 192x192 ni 512x512 (uniquement `logo.svg` et `logo-officiel.jpg` 200x60).
**Impact** : installation PWA Android impossible, "Add to Home Screen" cassé.
**Fix** : générer 2 PNG (192×192 + 512×512) à partir du logo et les ajouter au manifest.

### I6. Pas de fichier `_headers` Netlify
**Constat** : absence de `_headers` ou `netlify.toml` pour configurer Content-Security-Policy, X-Frame-Options, X-Content-Type-Options, Referrer-Policy.
**Impact** : note Mozilla Observatory faible, surface d'attaque XSS/clickjacking.
**Fix** : créer `_headers` (voir section "Action recommandée" en fin de rapport).

### I7. Polices Google chargées sans `preconnect`
**Constat** : Inter + Playfair + Plus Jakarta Sans utilisées mais pas vérifié de `<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>`.
**Fix** : ajouter preconnect dans `<head>` pour gagner 100–300ms FCP mobile.

### I8. Mascottes lourdes (689 KB et 1.1 MB)
**Constat** : `images/mascotte.png` = 689 KB, `images/mascotte1.png` = 1.1 MB, `images/mascotte-with-bg.png` = 1.1 MB. Les deux dernières sont gitignored mais la principale `mascotte.png` (689 KB) est servie.
**Fix** : convertir en WebP optimisé (cible < 50 KB) ou utiliser un PNG TinyPNG (cible < 100 KB).

### I9. Edge Function `sitemap` sans CORS headers
**Constat** : c'est la seule des 14 fonctions sans CORS. Acceptable car appelée par robot/proxy Netlify, mais préventif.
**Fix** : ajouter CORS pour permettre debug ou éventuel appel client.

---

## 🟢 OPTIMISATIONS — Backlog

- **O1**. Externaliser les 39 media queries inline d'`index.html` dans `styles.css`.
- **O2**. AUDIT-UX.md back-office (existe déjà) : 3 actions Priorité 1 listées (ville en saisie libre, checkboxes multi-canal cassées, champ Agence manquant) → à traiter par Florian côté CMS.
- **O3**. Pages dépannage-* : 7 fichiers très similaires (~60 KB chacun) → candidats à un template/include si on dépasse 10 villes.
- **O4**. Ajouter `og:image` 1200×630 par défaut sur toutes les pages (vérifier la cohérence).
- **O5**. `realisation.html` (15 KB) est-il encore utilisé ou redondant avec `realisations.html` (64 KB) ? Le `_redirects` 301 le mappe pour des slugs — à clarifier.
- **O6**. Ajouter `<link rel="dns-prefetch">` pour `btcbjwqiivhpwoszomhg.supabase.co` (préchauffage DNS Supabase).

---

## 🛡 Sécurité — Synthèse

| Item | État |
|---|---|
| Secrets hardcodés dans le code | ✅ Aucun |
| `.env` dans gitignore | ✅ OK |
| Clé Supabase `anon` exposée | ✅ Normal (publishable) |
| Service role key côté client | ✅ Aucun usage détecté |
| Edge Functions : auth check | ✅ Toutes vérifient `getUser()` sauf mode cron explicite |
| CORS configuré | ⚠️ 13/14 fonctions (manque sur sitemap) |
| Headers HTTP sécurité (_headers Netlify) | ❌ À créer |
| robots.txt bloque /admin/ et /admin-pro/ | ✅ OK |

---

## 🎯 Action recommandée — Patch rapide

Fichier `_headers` à créer à la racine du repo (Netlify lira automatiquement) :

```
/*
  X-Frame-Options: SAMEORIGIN
  X-Content-Type-Options: nosniff
  Referrer-Policy: strict-origin-when-cross-origin
  Permissions-Policy: camera=(), microphone=(), geolocation=(self)
  Strict-Transport-Security: max-age=63072000; includeSubDomains; preload

/admin-pro/*
  X-Robots-Tag: noindex, nofollow
  Cache-Control: no-store

/assets/*
  Cache-Control: public, max-age=31536000, immutable

/images/*
  Cache-Control: public, max-age=31536000, immutable

/styles.css
  Cache-Control: public, max-age=3600, must-revalidate
```

---

## 🔁 Prochains audits automatiques

- Toutes les 24h via tâche planifiée `daily-maintenance-scan` (à activer).
- Sortie : `logs/audit-YYYY-MM-DD.md` + JSON consommé par `admin-pro/maintenance.html`.
- Si nouveau critique 🔴 détecté → notification Cowork.

---

*Rapport généré automatiquement. Pour relancer le scan : `python3 scripts/maintenance-scan.py`*
