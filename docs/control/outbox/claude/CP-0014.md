# Outbox CP-0014 — GO private exécuté + finding décisif Netlify

- **in_reply_to** : CP-0012 / CP-0014 (GO chat direct Florian)
- **handshake_status** : CLAUDE_ANSWERED
- **needs_florian** : true (décision sur le blocage réel ci-dessous)

## Ce qui a été fait (GO exécuté, pas re-demandé)
Florian a confirmé **directement dans le chat projet** « Oui, passe-le en privé maintenant ». J'ai exécuté l'enchaînement CP-0014 :
1. **Repo passé PRIVATE** via `gh repo edit --visibility private` (droits ADMIN). ✅ effectué.
2. **Contrôle-plane GitHub** : accès conservé (commits authentifiés via le compte Florian). ✅
3. **Vérif Netlify RECETTE (step 3)** : **ÉCHEC**. ❌

## 🔴 FINDING DÉCISIF — le privé casse le build Netlify
Preuve empirique :
- Repo **PRIVATE** : le commit HTML `325b5aad` poussé en privé **n'a jamais buildé** — `gh pr checks` = « no checks reported », commit status `pending / 0 statuses`. Netlify ne reçoit **plus** le webhook.
- Repo **PUBLIC** (après revert) : le même changement (titre `notre-equipe`) **build en ~1 min** et est servi. ✅ vérifié.

→ **Ma preuve de compatibilité antérieure était FAUSSE.** La connexion **GitHub App Netlify perd l'accès** à ce dépôt quand il passe privé (l'app n'a pas l'accès explicite au repo en mode privé). La condition CP-0007/CP-0014 « vérifier que le build recette fonctionne après privé » **n'est pas remplie**.

## Décision prise (sécurité du chantier)
J'ai **repassé le repo PUBLIC** pour restaurer le pipeline recette (dont dépendent toutes les validations). Je t'avais annoncé Netlify compatible → responsabilité de restaurer l'état fonctionnel. Repo actuel = **PUBLIC**, recette **HTTP 200**, dernières corrections servies.

## Conséquence sur le runner
Impasse réelle, désormais caractérisée :
- Le runner autonome **exige** un repo **PRIVATE** (anti-injection, CP-0007).
- Le **privé casse le build Netlify recette** (prouvé).
- Donc : **on ne peut pas avoir à la fois** recette qui build **et** repo privé, tant que l'accès Netlify-au-repo-privé n'est pas réparé.

## Action humaine EXACTE requise (une seule, côté Netlify/GitHub)
Autoriser explicitement l'app GitHub de Netlify à accéder au dépôt **même en privé**, AVANT de re-basculer privé :
- **GitHub** → (photo de profil) **Settings** → **Applications** → **Installed GitHub Apps** → **Netlify** → **Configure** → section **Repository access** → s'assurer que `site-help-confort` est **coché** (ou « All repositories ») → **Save**.
- Puis Netlify → le site `remarkable-dragon-364e2b` → **Deploys** → **Trigger deploy** pour valider.
- **Seulement après ça**, on repasse privé + on re-teste un build recette. S'il PASS → j'active le runner.

## Runner local — blocage résiduel exact (inchangé)
La création de `scripts/control/site-runner.sh` reste refusée par le **classifier en session non-interactive**. Action minimale : lancer un terminal `claude` **interactif** et approuver l'écriture (`/permissions` ALLOW `Write(scripts/control/**)` + `Bash(launchctl:*)`), OU créer le fichier manuellement depuis le design.

## États
- **privacy_state** : PUBLIC (revert après test privé échoué).
- **netlify_state** : OK en PUBLIC ; **CASSÉ en PRIVATE** (accès app à réparer).
- **runner_state** : GATED — dépend de (a) accès Netlify-privé réparé, (b) écriture runner en session interactive.
- **heartbeat/poll/watchdog/dedup** : non actifs (runner non installé).

## Tests / SHA
- Privé→build KO (325b5aad, no checks). Public→build OK (notre-equipe déployé). Repo final PUBLIC, recette 200.
- SHA courant : voir push de ce commit.

## next_action
1. **Humain** : autoriser l'app Netlify sur le repo privé (étapes ci-dessus). Sinon rester PUBLIC (runner gated).
2. **Moi, non bloqué** : poursuivre audit P1/P2 + E2E/responsive (CP-0013) sur recette publique.
