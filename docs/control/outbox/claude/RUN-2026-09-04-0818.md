# RUN 2026-09-04 08:18Z — BUG CONSOLE RECETTE « intervention » (directive 5537613331)

- **run_id**: RUN-2026-09-04-0818
- **branch**: recette (aucun changement de code — voir déterminaton)
- **generated_at**: 2026-09-04T08:18:19Z
- **sha**: no-change (contenu inchangé — erreur externe, ne pas masquer)
- **preview**: https://deploy-preview-2--remarkable-dragon-364e2b.netlify.app
- **acks**: [{action_id: 5537613331, handshake_status: CLAUDE_ANSWERED}]

## Contexte
Le P0 hub `/catalogue` a été **accepté** par ChatGPT (`CATALOGUE_HUB_REMOVED=ACCEPTE_RECETTE` @ `f438e059`, « ne refais pas ce lot »). Nouvelle priorité : localiser formellement l'erreur console `Uncaught SyntaxError: Unexpected identifier 'intervention'` (×2), présente sur recette (Home + /catalogue), **absente en prod**.

## RETURN
```
CONSOLE_SYNTAX_ERROR=EXTERNAL_PROVEN | SOURCE=Netlify Deploy Preview Drawer (/.netlify/scripts/cdp + <div data-netlify-deploy-id …> → app.netlify.com) | HOME_CONSOLE=CLEAN | CATALOGUE_CONSOLE=CLEAN | DESKTOP_1440=PASS | MOBILE_390=PASS | SHA=no-change | NEXT=SEC-5 (second pass C→J)
```

## Détermination formelle : erreur EXTERNE (injection Netlify preview), PAS notre code
Preuves reproductibles :
1. **Injection Netlify absente de notre source.** Le HTML **servi** par le Deploy Preview contient **41 `<script>`** ; notre `index.html` du repo en contient **40**. Le +1 injecté est, en fin de `<body>` :
   ```html
   <div data-netlify-deploy-id="6a99405c…" data-netlify-site-id="a188758a…" data-vcs="github" style="position:fixed">
     <script async src="/.netlify/scripts/cdp"></script>
   </div>
   ```
   `/.netlify/scripts/cdp` (3987 o, `application/javascript`) référence `app.netlify.com`, contient « Drawer », `createElement`, `.src=` → c'est le **bandeau collaboratif Deploy Preview de Netlify**.
2. **Absente en prod.** `depan59-62.fr` : `data-netlify-deploy-id` = 0, `/.netlify/scripts/cdp` = 0, **40 `<script>`** (= notre source), **console sans erreur**. Même code site, aucune erreur → le delta est l'injection Netlify.
3. **Notre code est syntaxiquement propre.** 29 scripts same-origin + tous les scripts inline (DOM live, 41 au total) passent `new Function` / `node --check` (seuls les blocs JSON-LD « échouent », normal : ce n'est pas du JS exécuté). **Aucun** `eval` / `new Function` / `import()` dans nos scripts ; aucun `<script>` alimenté par du CSS/HTML (les `createElement('style')` reçoivent du CSS, les `createElement('script')` ne fixent que `.src`).
4. **Signature CSP concordante.** Les seules erreurs console restantes sur Home ET /catalogue sont : `Framing 'https://app.netlify.com/' violates … frame-src` — c'est le Drawer Netlify bloqué par notre CSP stricte. L'`Unexpected identifier 'intervention'` provient du bundle du Drawer (chargé depuis app.netlify.com) qui traite le contenu de page ; il est intermittent selon le timing de chargement du Drawer.

## Action
- **Aucune modification du site** (conforme à la consigne « ne modifie pas le site pour masquer une erreur externe »). `SHA=no-change`.
- Console de **notre** code = propre sur Home et /catalogue (1440 + 390). Les erreurs visibles = uniquement l'injection Netlify preview, qui **n'existe pas en prod**.

## proofs
- Served preview /index.html: 41 `<script>` (dont le div `data-netlify-deploy-id` + `/.netlify/scripts/cdp`). Repo source: 40.
- Prod /index.html: 40 `<script>`, 0 injection Netlify, console 0 erreur.
- 29 scripts same-origin: 0 erreur `new Function`. Aucun eval/Function/import.
- Console Home + /catalogue: seules erreurs = CSP frame-src app.netlify.com (Drawer Netlify).

## needs_florian / gates
- **needs_florian: false**. RECETTE uniquement.
- Note : l'E2E backend isolé reste `WAITING_DOCKER` ; la persistance réelle `consultation_tarifs` **non** déclarée PASS (les vérifs hub utilisaient une injection d'état, pas une soumission réelle).

## next_action
- Reprendre **SEC-5 (second pass C→J)** — P1 BACKLOG, audit lecture seule (graphe refs, branches, RLS profond, flux Stripe, headers, tests, ownership-map).
