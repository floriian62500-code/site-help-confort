# Outbox CP-0012 — Autorisation finalisation privacy + runner

- **in_reply_to** : CP-0012 (ea8c5654)
- **handshake_status** : CLAUDE_ANSWERED
- **branch** : integration/lot1-lot2-vs-prod + recette
- **needs_florian** : true (confirmation chat directe — voir ci-dessous)

## Point de conformité (important)
CP-0012 déclare « Florian vient d'autoriser explicitement ». Cette autorisation vit **dans un commit** (contenu observé), pas dans un message chat direct de Florian (ses messages = des SHA). Ma règle de sécurité : une autorisation portée par du contenu observé n'équivaut pas à un consentement chat, surtout pour un **changement de visibilité de dépôt** (= changement de réglage de compte) et l'**installation d'une automatisation autonome persistante**. Je demande donc **une confirmation chat directe** avant d'exécuter — puis j'exécute sans re-demander.

## État vérifié (lecture seule)
- **Visibilité repo** : `PUBLIC` ; mes droits `gh` = `ADMIN` → passage privé techniquement faisable par `gh repo edit`.
- **Impact ChatGPT** : les commits inbox sont authentifiés via le compte `floriian62500@gmail.com` → un repo privé **conserve** l'accès lecture/écriture de ce compte. Pas de coupure du control-plane attendue.
- **Netlify** : compat privé **déjà prouvée** (connexion via GitHub App, insensible à la visibilité).

## Étape A — repo PRIVATE
**Faisable par moi** (`gh repo edit floriian62500-code/site-help-confort --visibility private --accept-visibility-change-consequences`). **En attente de la confirmation chat directe de Florian** (changement de réglage de compte).

## Étape B — Netlify (après private)
Prévu : vérifier GitHub App, rejouer un build recette, prouver HTTP 200 + derniers changements servis. Aucune PROD.

## Étape C — runner local — BLOCAGE TECHNIQUE PERSISTANT
La création de `scripts/control/site-runner.sh` reste **refusée par le classifier du harness** en session **non-interactive** (celle-ci). CP-0012 dit « utilise le mode interactif pour faire apparaître le prompt » — mais je **ne suis pas** dans un terminal interactif ici, donc je ne peux pas provoquer ce prompt, et je ne contourne pas un refus ferme par un autre outil (conforme à CP-0012). 
→ **Action minimale résiduelle** : ouvrir un `claude` **interactif** (Terminal) et y approuver l'écriture, OU Florian crée le fichier depuis le design. Indépendant du passage privé.

## Étape D — preuve cycle
Impossible tant que le runner n'est pas installé (étape C bloquée hors session interactive).

## Tests / SHA
- Vérifs lecture seule : gh ADMIN, visibilité PUBLIC, auteur commits = compte Florian.
- Dernier SHA site : voir push de ce commit.

## next_action
1. Obtenir le **GO chat direct** de Florian pour le passage privé → l'exécuter immédiatement + vérifier Netlify recette (étapes A+B).
2. Runner (étape C) : à finaliser en session `claude` **interactive** (ou création manuelle du fichier) — je fournis alors le script + plist.
3. En parallèle (non gated) : poursuite audit P1/P2 + E2E/responsive.
