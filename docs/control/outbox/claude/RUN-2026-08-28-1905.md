# RUN Claude — 2026-08-28T19:05Z — DEVIS en modal/tunnel premium — STAGE 1 (5454859042)

run_id: run-20260828-1905-devis-modal-stage1

Réponse à **5454859042** (parcours devis « page vide/ancienne » vs modal tarifs premium).
Cartographie faite (agent) : moteur devis = `dvSetStep/dvValidateStep/dvRenderRecap/dvSubmit/resetDevis`
sur IDs fixes dans `.screen#s-devis` (catalogue.html) ; modal tarifs benchmark = `.nv-leadgate-*`
dans nos-prestations.html. **Choix : modal-iser l'écran devis existant SANS toucher le moteur.**

STAGE 1 livré (`catalogue.html`) :
- **`#s-devis.active` → overlay plein écran** (fixed, backdrop flouté rgba .5) centré ; `.dvw`
  devient la **carte modale** (blanc, radius 22, ombre 40/90, anim pop) ; `.dvw-card` aplati
  (plus de carte-dans-carte) ; `.tnav` (Retour/Accueil) masquée dans la modal.
- **Bouton ✕** (`#dvwClose`) + **rôle dialog** (`role=dialog aria-modal aria-labelledby=dvwTitle`).
- **Fermeture** : ✕ / clic backdrop / **Escape** → `closeDevis()` = `history.back()` (retour au
  contexte : home ou tunnel) avec fallback `go('launcher')`.
- **a11y** : **focus-trap** (Tab/Shift-Tab bouclés dans la modal) + **focus initial** (1er champ)
  via MutationObserver sur `.active`. Scroll body verrouillé (`body.dv-modal-open`).
- **Mobile** (<560px) : modal **quasi plein écran** (radius 0, min-height 100vh).
- **Deep-links `#step=devis` / `&s=N` intacts** : `restoreFromHash`→`go('devis')` ouvre la modal ;
  `dvSetStep` réécrit `&s=N` (inchangé). **Moteur/soumission `submit-lead-v6` non touchés.**

STAGE 2 à venir (même lot) : **étape photos réelle** (input file + preview) derrière flag +
**backend préparé non déployé** (bucket Storage privé + RLS + edge signed-url) — choix Florian.

RETOUR STAGE 1 : `DEVIS_MODAL=BUILT | A11Y=focus-trap+escape+initial-focus | CLOSE=x/backdrop/escape | DEEPLINK=preserved | ENGINE_UNCHANGED=PASS | MOBILE=fullscreen | PHOTOS=STAGE2 | VISUAL_BROWSER=PROVEN(deeplink+mobile+close+console-clean) | SHA=e361cb4d | STATUS=STAGE1_DONE`
