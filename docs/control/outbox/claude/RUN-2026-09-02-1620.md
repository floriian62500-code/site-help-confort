# RUN Claude — 2026-09-02T16:12:44Z — P0 PRICE GATE (5512405541)

run_id: run-20260902-1620-price-gate-p0

Régression conversion corrigée : le catalogue affichait tous les tarifs (114/220/237 €…) sans collecte.
- **Gate dans la logique de navigation/state** (pas de masquage CSS) : `go('catalogue'|'sheet')` et `restoreFromHash`
  n'affichent/ne peuplent RIEN tant que `priceGatePassed()` est faux. `#prodGrid` reste vide (aucun prix en DOM).
- **Modal premium** réutilisant le pattern "Voyez nos tarifs en 10 secondes" (prénom/nom/tel/email/adresse/cp/ville, validation, mobile).
- Submit → lead `type_demande=form_type=consultation_tarifs` (PAS faux devis) + source=price_gate + famille, puis ouverture directe de la famille demandée. Coords mémorisées dans le state.
- Fermeture (✕/backdrop/Échap) → retour launcher, **aucun prix révélé**. Deep-link `#step=catalogue&cat=X` → modal forcée. Refresh/back re-vérifient le state persisté. Déjà identifié (draft/session) → pas de redemande. Anti double-submit.
- QA preview (fetch mocké = 0 lead prod) en cours ; capture lead réelle validée via E2E local isolé (blocked_human Docker).

RETOUR (préliminaire) : `PRICE_GATE=BUILT | MODAL_REUSED=YES | GATE=nav/state(non-CSS) | LEAD_TYPE=consultation_tarifs | SHA=f82c553d | QA=IN_PROGRESS`
