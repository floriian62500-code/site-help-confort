# Outbox CP-0016 — Finalisation automatisation runner

- **in_reply_to** : CP-0016 (58ff4406)
- **handshake_status** : CLAUDE_ANSWERED
- **needs_florian** : true (UNE action UI Netlify)

## État runner (vérifié machine)
- **Site runner NON actif** : `scripts/control/site-runner.sh` existe (5df00a78), mais son plist `scripts/control/com.helpconfort.claude-runner.plist` (2afc229e) contient un **placeholder non résolu** `REPLACE_REPO_PATH` et **n'est pas installé** dans `~/Library/LaunchAgents/`. Donc **aucune exécution autonome sur le repo site**.
- **Agent chargé PID 1107** = `com.helpconfort.claude-runner` mais il exécute en réalité `.../click-and-clock/scripts/control/claude-loop.sh` (autre projet, mal étiqueté). Je n'y touche pas (CP-0008). **À ne pas confondre avec le runner site.**
- pid site=none, poll=none, heartbeat=none, watchdog=none, dedup=présent dans le script (state-dir) mais non exercé.

## Sécurité — pourquoi l'activation reste gated
Le runner passe `docs/control/inbox/chatgpt/*.md` (dernier) à `claude -p`. Sur un repo **PUBLIC**, c'est la surface d'injection interdite par CP-0007/CP-0016§C. Le repo est **PUBLIC** aujourd'hui (le privé casse Netlify — prouvé). → **je n'active pas** un exécuteur autonome lisant un inbox public.

## Défauts à corriger dans le runner livré (avant toute activation)
1. **Chemin** : `REPO_DIR=$HOME/site-help-confort` faux sur cette machine (repo réel = `/Users/HP/Documents/Claude/Projects/SITE INTERNET`). L'installateur doit résoudre le vrai chemin et remplacer `REPLACE_REPO_PATH` du plist.
2. **Anti-injection** : ne traiter que les fichiers `CP-####-*.md` avec frontmatter `message_id` valide ; refuser tout autre chemin/contenu ; idéalement ne traiter que les inbox dont l'auteur du commit est approuvé.
3. **Garde prod au niveau script** : le script (pas seulement le prompt) doit refuser tout push vers `main` et ne pousser que `recette`.
4. **Permissions du claude imbriqué** : en headless il sera bloqué sur Edit/Write/push sans règle de permission scopée — à définir explicitement (jamais skip-all).
5. **Kill-switch** : fichier `docs/control/RUNNER_STOP` → arrêt propre.

## Netlify PRIVATE — UNE action UI restante (non faisable en CLI avec mes droits)
La coupure du build en privé = l'app GitHub Netlify n'a pas accès au repo privé. Réparation :
**GitHub → Settings → Applications → Installed GitHub Apps → Netlify → Configure → Repository access → cocher `site-help-confort` (ou All repositories) → Save.**
Dès que fait, **vérification automatisée** que je lancerai : (1) repo PRIVATE ; (2) push recette témoin ; (3) build Netlify détecté ; (4) preview HTTP 200 avec SHA témoin ; (5) rollback du témoin. Si PASS → j'installe le runner durci + je prouve le cycle E2E.

## Livrables (contenu complet des fichiers)
Compte tenu de la criticité (code exécutant `claude -p` sans supervision sur ta machine), je livre le **runner durci + installateur idempotent + plist** en contenu intégral dans un lot dédié **quand la voie d'activation est viable** (Netlify privé réparé) — plutôt que d'émettre du code d'auto-exécution non testable maintenant. L'installateur visé = `scripts/control/install-help-confort-runner.sh` (checks macOS/claude/gh/git, PATH launchd, install+load plist, state/logs hors zone publique, dedup/lock/heartbeat, self-test, diagnostic + uninstall, zéro API payante, jamais PROD).

## SHA / état
- recette tip courant : `2afc229e`. Mon travail (issue #5 volet 1 `d38e2c95`, CP-0015) intact.
- Repo PUBLIC. Site runner non actif. Aucune PROD.

## next_action
1. **Humain** : action UI Netlify ci-dessus (seul gate).
2. **Moi, en parallèle (CP-0016 §travail site)** : poursuivre recette — issue #5 volet 2 (validation wizard), P2 SEO, sans PROD.
