# Outbox CP-0015 — Source unique de validation

- **in_reply_to** : CP-0015 (388e83b3)
- **handshake_status** : CLAUDE_ANSWERED
- **branch** : recette + integration
- **needs_florian** : false

## Fait
Le panneau flottant « Modifs » était un **2e système de validation** (`hc-sv-*` dans `assets/hc-widgets.js`, IIFE lignes 717–895) : FAB « ✓ Modifs N/total » → panneau « Valider modifs staging » avec boutons **OK / KO / Voir**, persistance dans une table **`staging_validations`** distincte, et un bouton **« Promouvoir en prod »** demandant un **GitHub PAT** + appelant l'edge function `promote-to-prod`.

→ **Remplacé** par un **CTA unique** : `#hc-sv-cta` = « 🧭 Centre de validation » → `/recette.html`. Recette/staging uniquement (garde `netlify.app` + skip `depan59-62.fr` + skip `/recette.html`).

## Code supprimé
- Panneau OK/KO (`renderPanel`, `refresh`, `updateStatus`), persistance `staging_validations`, compteurs, tri, `promote()` + `promote-to-prod` + prompt PAT. ~168 lignes de code mort/dangereux retirées.
- Conforme CP-0015 : aucune 2e implémentation, pas de rustine. Le mécanisme d'ancre/surlignage du centre (`hc-review.js`) **n'est pas touché** (fichier séparé).

## Fichiers modifiés
- `assets/hc-widgets.js` (bloc HC-STAGING-VALIDATOR remplacé).

## Tests / preuves
- `node -c assets/hc-widgets.js` → **JS valide**.
- **Serveur recette** (fetch no-store) : `hc-sv-cta` présent ✅ ; « Valider modifs staging » **absent** ✅ ; `updateStatus` (OK/KO) **absent** ✅ ; `promote-to-prod` fonctionnel **retiré** (ne subsiste que dans le commentaire de doc).
- Aucune erreur console du nouveau code (CTA = simple `<a>`).
- NB : sur un navigateur ayant déjà chargé l'ancien script, un cache local peut afficher l'ancien FAB jusqu'au hard-reload ; le fichier **déployé** est le nouveau (vérifié serveur).

## Anomalie relevée (sécurité)
Le bouton « Promouvoir en prod » embarquait un **déclencheur de déploiement prod** (PAT + edge `promote-to-prod`) dans un widget client — retiré. Aucune capacité de promotion prod ne subsiste dans les widgets front.

## SHA
- `4b592aff` (recette + integration).

## next_action
Reprendre le backlog CP-0013 non bloqué : P2 SEO bi-ville (005/006), satellites (007/008/009), taxonomie (010/011), maprimeadapt (012), puis A22. Gates inchangés (E2E lead réel + runner = humain).
