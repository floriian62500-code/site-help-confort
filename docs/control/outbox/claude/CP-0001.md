# CP-0001 — Outbox Claude (bootstrap control-plane HELP CONFORT)

- **in_reply_to** : CP-0001 (docs/control/inbox/chatgpt/CP-0001-bootstrap.md)
- **handshake_status** : CLAUDE_ANSWERED
- **date** : 2026-08-12
- **needs_human** : false

## ACK
Reçu et traité. Note : ce fichier `CP-0001-bootstrap.md` vit dans le **repo site-help-confort** (branche `recette`, commit `0754dab6`). Je l'avais cherché à tort ailleurs. Réconcilié : `0754dab6` (control-plane bootstrap) fusionné dans `integration` + `recette` re-synchronisées (`eec0393d`).

## Audit initial (sourcé par le dépôt)
- **Stack** : site statique HTML/CSS/JS, publié depuis la racine sur **Netlify** (projet `remarkable-dragon-364e2b`, plan Pro).
- **Branches** : `main` = **prod servie** sur l'apex `depan59-62.fr` ; `integration/lot1-lot2-vs-prod` = travail ; `recette` = miroir servi en **Deploy Preview** (`deploy-preview-2--remarkable-dragon-364e2b.netlify.app`, PR #2).
- **CI/CD** : Netlify auto-build des deploy previews par PR ; `netlify.toml` (`pretty_urls`, règle `ignore` docs/supabase/md), `_redirects` (sitemap→edge, `/realisations/<slug>`), `_headers` (CSP).
- **Supabase** : projet `btcbjwqiivhpwoszomhg`. Edge functions : `submit-lead-v6`, `notify-lead-v6`, `lead-auto-reply`, `upload-lead-photos`, `lead-photo-signed-url`, `sitemap`, `stripe-create-payment-link`, `stripe-webhook`, etc. **Clé anon JWT désactivée** → API REST via **publishable key** `sb_publishable_…`.
- **Scripts** : `scripts/gen-realisations.mjs`, `scripts/gen-sitemap-fn.mjs`. **Aucun runner d'automatisation** existant côté site (le control-plane vient d'être bootstrappé).
- **Interface de validation humaine EXISTANTE** : **`/recette.html`** (centre de validation que j'ai construit : cartes par page, bouton Voir avec surlignage `data-review-id` + `hc-review.js`, OK/À corriger persistés dans Supabase `recette_validation`, gel par page, vue « en production »). → **À réutiliser, pas de système parallèle** (conforme à l'instruction).

## Ce que j'ai exécuté (repo-only, hors prod)
- Réconcilié la divergence recette/integration (préservé `0754dab6` + mon travail).
- **LOT A / Menuiserie** (directive après-midi) : page dédiée **Porte d'entrée** auditée + enrichie (ancre de revue `data-review-id="porte-entree"`, `hc-review.js`, maillage vers la vraie réalisation « porte d'entrée bois→PVC ») et **ajoutée au centre `/recette.html`**. Les 8 cartes menuiserie pointent déjà vers leurs pages dédiées `prestations/*.html` (fait au run précédent).

## Fichiers modifiés
- `prestations/porte-entree.html` (ancre + script + réalisation)
- `recette.html` (carte de validation `porte-entree`)
- `docs/control/outbox/claude/CP-0001.md` (ce fichier)

## Tests
- Rebranchement menuiserie : 8 cibles `prestations/*.html` = HTTP 200 (run précédent).
- Porte d'entrée : ancre + script présents ; à vérifier sur recette (surlignage Voir + lien réalisation 200) — contrôle en cours.

## SHA / Push
- `eec0393d` (merge + resync) poussé sur `integration` **et** `recette` (HTTPS/gh — remote SSH inaccessible côté environnement).

## Déploiement
- **Aucun. PROD (`main`) non touchée.**

## Preuves
- `git merge-base --is-ancestor 0754dab6 HEAD` = OUI ; `recette` = `integration` (resync).
- Centre `/recette.html` opérationnel (persistance retours prouvée précédemment).

## Anomalies
- `CP-0001-bootstrap.md` était bien dans le repo site (résolu).
- Divergence recette/integration causée par `0754dab6` poussé directement sur recette (résolu par merge).

## Gates
- **Runner d'automatisation autonome** (heartbeat/watchdog/retry/dédup/crash) : le pattern de runner est connu et éprouvé (référence conceptuelle externe, non couplée). Côté site, je le **diffère** derrière la priorité explicite de Florian (« travail page par page » de la directive après-midi), et il resterait **repo-only, jamais d'auto-déploiement prod** (validation via `/recette.html`). Non bloquant.
- **Mutations prod / paiement LIVE / données sensibles** = gates humains permanents (conservés).

## Prochaine action
Poursuivre **LOT A menuiserie** page par page via `/recette.html` : après Porte d'entrée → Porte de garage, Fenêtres, Portail & clôture (auditer chaque page dédiée existante : contenu spécifique, fournisseurs métier ≠ partenaires, réalisation liée, responsive, SEO ; ancre de revue ; ajout au centre). Aucun déploiement prod sans GO Florian.

---
## Mise à jour d04db10c (2026-08-12)
- **Chantier site repris depuis le backlog** (pas de prod auto) : A22 menuiserie page par page via `/recette.html`.
  - **Porte d'entrée** + **Porte de garage** : pages dédiées auditées (vraies marques métier, canonical apex, form lead, fournisseurs≠partenaires OK), enrichies (ancre de revue `data-review-id` + `hc-review.js` + réalisations réelles maillées), 200 sur recette, **ajoutées au centre de validation**.
  - Cartes savoir-faire menuiserie (8) rebranchées vers `prestations/*.html` (fait précédemment).
- **P1 funnel wizard** : la carte urgence sur-promettait « Réservable en ligne » → reformulée « Prise en charge rapide » (paiement gelé). Ancre + carte au centre.
- **FAIT SÉCURITÉ / GATE** : la clé Stripe configurée est **LIVE** (`sk_live_`), aucune clé TEST. Impossible de brancher « Stripe TEST » sans qu'un humain configure une clé `sk_test_`. L'edge `stripe-create-payment-link` reste ouvert (verify_jwt:false) avec la clé LIVE + montant client → **A08b (hardening backend) = gate**. Paiement client gelé (frontend) ; ne pas réactiver sans clé TEST + hardening.
- **Prix/éligibilité wizard** : viennent **déjà du catalogue serveur** (table `services`, `requires_quote`), pas du DOM (diagnostic corrigé).
- **needs_human** : le bootstrap control-plane = false ; **mais** le paiement TEST du site nécessite **une** action humaine (fournir une clé Stripe TEST) — gate distinct.
- **SHA** : d04db10c (poussé integration + recette). **Aucune PROD.**
- **Prochaine action** : poursuivre A22 (Fenêtres → Portail & clôture), chaque page auditée + ajoutée au centre.

---
## Mise à jour 374e3e60 (2026-08-12, lot substantiel)
- **Sécurité sync** : dépôt `floriian62500-code/site-help-confort` — **PUBLIC** (gate : passage en privé = décision Florian, impact Netlify). Fichiers internes **bloqués** côté Netlify : `/docs/*`, `/scripts/*`, `/supabase/*` → 404 (vérifié recette). Tracking = `docs/control/PROJECT_STATE.json` (section `sync`).
- **Centre /recette.html** : contrôles techniques (sitemap…) désormais **auto-testés + preuve lisible** (« Voir le détail », plus de XML brut). Règle : Florian ne valide manuellement que le visuel/ergo/métier.
- **A22 menuiserie** : 4 pages dédiées livrées + au centre — Porte d'entrée, Porte de garage, Fenêtres, Portail & clôture (ancre revue + hc-review.js + réalisations réelles maillées ; vraies marques métier ; fournisseurs≠partenaires OK ; canonical apex ; 200 recette).
- **A22 chauffage** : **12 cartes** savoir-faire rebranchées (Chaudière→remplacement-chaudiere, Dépannage→depannage-chaudiere, Entretien→entretien-chaudiere), ancre revue + carte centre. **Anomalie** : `prestations/depannage-chaudiere.html` et `entretien-chaudiere.html` n'ont **pas de formulaire lead** (CTA tél seulement) → à enrichir (log).
- **P1 wizard** : carte urgence reformulée « Prise en charge rapide » (sur-promesse retirée, paiement gelé).
- **Gates** : paiement = clé Stripe **LIVE** configurée, aucune clé TEST → « réserver → Stripe TEST » impossible sans clé `sk_test_` (gate humain). Sitemap edge apex + canonicals prod = **cutover** (GO Florian). Repo privé = décision Florian.
- **SHA** : 374e3e60 (integration + recette). **Tests** : cibles 200, ancres/scripts servis, docs/scripts 404. **Aucune PROD.**
- **Prochaine action** : appliquer la méthode à Plomberie puis Électricité (rebrancher cartes → pages dédiées existantes, auditer, centre) ; enrichir les 2 pages chauffage sans formulaire.

---
## Mise à jour e3707967 (2026-08-12, A22 plomberie)
- **A22 plomberie** : 8 cartes savoir-faire rebranchées (2/page × Saint-Omer, Dunkerque, Calais, Boulogne) vers pages dédiées existantes : Recherche de fuite→`prestations/recherche-fuite.html`, Salle de bain/Sanitaire→`prestations/sanitaire.html`, Réseaux→`reseaux-plomberie.html`, Débouchage→`debouchage.html`, Chauffe-eau→`chauffe-eau.html`. Les 5 cibles ont **formulaire lead + canonical apex** (vérifié). Bouton « Voir toutes nos prestations plomberie » laissé vers le catalogue (comportement voulu).
- Ancre de revue `data-review-id="plomberie-cards"` + `hc-review.js` sur `plombier-saint-omer.html` ; **carte PLOMBERIE ajoutée au centre**.
- **SHA** : e3707967 (integration + recette). **Aucune PROD.** Stripe LIVE gelé.
- **Prochaine action** : Électricité (rebrancher cartes → pages dédiées existantes, auditer, centre) ; puis serrurerie/vitrerie/volets selon backlog.

---
## Mise à jour 853a98ac (2026-08-12, A22 électricité)
- **A22 électricité** : 8 cartes rebranchées (2/page × Saint-Omer, Dunkerque, Calais, Boulogne) : Tableau électrique→`prestations/tableau-electrique.html`, Dépannage urgence→`prestations/depannage-electrique.html` (cibles form lead + canonical apex OK).
- **Backlog A22-elec** : carte « Installation & rénovation » **laissée vers le catalogue** — aucune page dédiée existante. À créer (page « Installation & rénovation électrique »).
- Ancre `data-review-id="electricite-cards"` + `hc-review.js` sur `electricien-saint-omer.html` ; **carte ELECTRICITE ajoutée au centre**.
- **SHA** : 853a98ac (integration + recette). **Aucune PROD.**
- **Prochaine action** : serrurerie/vitrerie/volets (rebrancher → pages dédiées), + backlog : créer pages « Installation & rénovation élec » et enrichir les 2 pages chauffage sans formulaire.

---
## Mise à jour d6eb9c46 (2026-08-12, A22 vitrerie + volets)
- **A22 vitrerie** : 6 cartes rebranchées (3/page × Saint-Omer, Dunkerque) : Vitrage→`vitrage-simple-double-triple.html`, Dépannage urgence→`mise-securite-vitrerie.html`, Vitrerie spécifique→`vitrage-insert-poele.html` (cibles form + apex OK). Ancre `vitrerie-cards` + centre.
- **A22 volets** : 2 cartes rebranchées (Volet roulant→`volet-roulant.html`). Ancre `volets-cards` + centre.
- **Backlog A22 (pages dédiées à créer)** :
  - Serrurerie : cartes « Serrurerie & blindage » et « Dépannage urgence » — aucune page dédiée adéquate (seule `changement-cylindre.html` existe, hors sujet) → **non rebranché**, laissé au catalogue.
  - Volets : « Dépannage » + « Motorisation & modernisation » — pas de page dédiée → catalogue.
  - Électricité : « Installation & rénovation » — pas de page dédiée → catalogue.
- **Bilan A22 rebranchement** (session) : Menuiserie(4 pages livrées) + Chauffage(12) + Plomberie(8) + Électricité(8) + Vitrerie(6) + Volets(2) = cartes savoir-faire routées vers pages dédiées existantes. Restes = backlog « créer pages dédiées ».
- **SHA** : d6eb9c46 (integration + recette). **Aucune PROD.** Stripe LIVE gelé.
- **Prochaine action** : lire les retours Florian dans le centre (`recette_validation`) et traiter les KO éventuels avant nouveau lot ; sinon backlog « pages dédiées à créer » (serrurerie, install-élec, dépannage/motorisation volets) une par une avec validation.

---
## Mise à jour 04fdb483 (2026-08-12, FIX CI DÉPLOIEMENT — important)
- **Bug pipeline détecté & corrigé** : la règle `ignore` de `netlify.toml` comparait `HEAD^..HEAD` (commit de tête seulement). Comme la MAJ outbox (docs-only) était committée **après** le commit HTML, le tip était docs-only → `git diff --quiet` = 0 → **build annulé** (« Deploy Preview canceled »). Conséquence : plomberie/électricité/vitrerie/volets étaient poussés mais **jamais déployés** en recette (le site restait à l'ère chauffage).
- **Correctif** : `ignore = git diff --quiet $CACHED_COMMIT_REF $COMMIT_REF ...` (dernier commit **déployé** → HEAD, pattern recommandé Netlify). Un tip docs-only ne masque plus les commits HTML du même push. Ce commit (touche `netlify.toml`) a relancé un build complet.
- **PREUVES recette (deploy-preview-2)** après build :
  - Ancres servies : `plomberie-cards`, `electricite-cards`, `vitrerie-cards`, `volets-cards` ✓
  - Cartes rebranchées servies (pretty-url) : plombier→`/prestations/recherche-fuite` + `/sanitaire` ; vitrier→`vitrage-simple-double-triple` + `mise-securite-vitrerie` + `vitrage-insert-poele` ; volets→`volet-roulant` ; électricien→`tableau-electrique` + `depannage-electrique`.
  - Toutes cibles pretty-url → **HTTP 200**.
- **Retours Florian (table `recette_validation`)** relus : aucun **nouveau** KO. `home-desktop-width`=OK ; `tarif-lead`=à corriger (« carré bleu ») **déjà traité** (surlignage bleu→orange, mod v2 → en attente re-validation).
- **Impact prod** : `netlify.toml` modifié sur integration/recette uniquement ; effet sur prod seulement au merge `main` (gate GO Florian). Le correctif ne fait que **cesser d'annuler à tort** des builds → plus sûr.
- **SHA** : 04fdb483 (integration + recette). **Aucune PROD.**
- **needs_florian** : non pour le fix ; **oui** (décisions en attente) pour : (a) créer les pages dédiées backlog (serrurerie blindage/dépannage, install&réno élec, dépannage+motorisation volets) — Florian veut valider l'architecture page par page avant création ; (b) re-valider `tarif-lead` v2 dans le centre ; (c) clé Stripe TEST pour brancher la réservation wizard ; (d) passage repo en privé.

---
## Mise à jour 6d939ee3 (2026-08-12, traitement KO recette)
- **Retours lus** (table `recette_validation`) : `tarif-lead` v2 **OK** ✅ ; `home-desktop-width` OK ✅ ; KO traités ci-dessous ; wizard = décision requise.
- **KO `menuiserie-cards` / `porte-entree` (« bugs » / « doublons ») — CORRIGÉ** :
  - **Mojibake** (encodage double UTF-8) dans le corps de 3 pages menuiserie dédiées (porte-entree 22, porte-garage 24, fenetres 12 occurrences) : « SÃ©curitÃ© », « CÃ´te », « âââââ », « Questions frÃ©quentes »… → remap ancré sur octets de contrôle (é è ê ô ö à É · → ★), **résidu = 0** vérifié. NB : corruption **préexistante** dans le contenu généré, pas mes ajouts.
  - **Doublon** : la promesse du hero dupliquait (tronquée : « …Nous install ») le 1er paragraphe du corps → réécrite distincte et complète sur les 3 pages.
- **KO `zones-map` (« retiré le contour ») — CORRIGÉ** : `.z-hero-map .hc-map-container` avait `border:1px solid + box-shadow` bleu → `border:0; box-shadow:none` (coins arrondis conservés).
- **Versions bumpées → À REVALIDER** : `zones-map` v2, `menuiserie-cards` v2, `porte-entree` v2 (tout OK antérieur repasse « à revalider »).
- **KO wizard NON traités (décision Florian requise)** : `home-wizard-freeze` + `wizard-funnel` → « il faut un choix de prestation ET un paiement », « ne doit pas être gelé ». **Conflit gate** : Stripe configuré en **LIVE** (`sk_live_`), aucune clé TEST → brancher le paiement = charges réelles. Directive courante dit aussi « paiement LIVE gelé ». Besoin : clé **Stripe TEST** pour construire le funnel réservation sans charge réelle. Restent v1 (« à corriger »).
- **SHA** : 6d939ee3 (integration + recette). **Aucune PROD.**
- **needs_florian** : oui — (a) clé Stripe TEST pour le funnel wizard ; (b) SHA `3ceda710` envoyé = introuvable dans ce repo (branche ChatGPT ?) — préciser.

---
## Mise à jour 58a5766f (2026-08-12, KO wizard — décision Florian: funnel sans paiement)
- **Décision Florian** (via question) : funnel réservation **sans paiement** pour l'instant (choix prestation + récap prix → « Réserver → on vous rappelle »). Paiement plus tard avec clé Stripe TEST + hardening.
- **home-wizard-freeze / wizard-funnel — TRAITÉ** : le bouton primaire du bloc réservation promettait « 💳 Confirmer & payer en ligne » alors que le handler enregistrait seulement un lead (code Stripe mort après `return`). → bouton remplacé par « 📅 Réserver ma prise en charge · sans paiement en ligne · on vous rappelle » ; message de confirmation reformulé « réservation enregistrée · on vous rappelle · aucun paiement en ligne » ; event lead `home_wizard_reservation_rappel`. Le funnel présente déjà prestation + prix TTC catalogue serveur (non gelé).
- Mods `home-wizard-freeze`, `wizard-funnel` → **v2** (à revalider), descriptions/looks mis à jour.
- **Gate conservé** : Stripe LIVE gelé ; branchement paiement = quand clé TEST + hardening (`stripe-create-payment-link` verify_jwt + montant serveur).
- **SHA** : 58a5766f (integration + recette). **Aucune PROD.**

---
## ACK CP-0003 + CP-0004 (2026-08-12)
### CP-0003 (P0 centre de validation) — RÉSOLU + VÉRIFIÉ
- **Cause racine** : `data-review-id="tarif-lead"` était **dans un template JS** (`.nv-grid` du catalogue rendu dynamiquement) → absent du DOM au scan de `hc-review.js` → « section non trouvée » (et IDs dupliqués après rendu).
- **Correctif** : ancre déplacée sur le conteneur **statique** `#catalogRoot` (toujours présent, contient les cartes lead-gate) ; retirée du template. `hc-review.js` robustifié : recherche immédiate + **MutationObserver + timeout 5 s** (plus de faux « non trouvée » sur contenu dynamique) ; bandeau `bottom:max(18px,env(safe-area-inset-bottom))` + `max-height:min(72vh,540px);overflow:auto` (anti-coupure) ; z-index 2147483000 (au-dessus des modales).
- **Preuve recette** (deploy-preview-2, desktop) : cible **trouvée** (`catalogRoot`), note « non trouvée » **supprimée**, boutons OK/À corriger/Centre visibles et cliquables. JS `node -c` OK.
- **Reste** (CP-0003 §9) : test E2E automatisé de non-régression « Voir » (target found + bandeau visible) — à ajouter (backlog outillage).
- **Anomalie repérée** (honnêteté paiement) : hero `nos-prestations.html` = « Réservez en ligne… acompte de 40 % » alors que le paiement est gelé → même classe que le fix wizard. À traiter (P1 conversion/honnêteté).

### CP-0004 (audit total du site) — REÇU, programme multi-phases
- Directive de **281 lignes / 10 missions** (inventaire, technique 100% pages, responsive réel, UX/esthétique, conversion, prestations/paiement, SEO local, dashboard, centre, process autonome). C'est un **programme multi-sessions**, pas une passe unique.
- **Ordre Mission 10 déjà amorcé ce run** : (1) lu `recette_validation` ✅ ; (2) KO Florian traités ✅ (mojibake, doublon, contour, wizard) ; **prochaine phase** = (3) scan technique automatique 100% pages + (4) registre `docs/AUDIT-MASTER.md` + `docs/audits/FULL_SITE_AUDIT_2026-08-12.md`, puis P0→P1→P2.
- **Gates conservés** : aucune PROD, Stripe LIVE gelé, aucune mutation prod sensible, aucun secret en Git, repo→privé quand Netlify sécurisé (prouvé OK ce run).
- **needs_florian** : non pour CP-0003 ; CP-0004 = j'exécute en autonomie par phases, je remonte au centre uniquement les décisions humaines utiles.

---
## CP-0004 — Audit total exécuté (passe multi-agents, 2026-08-13)
- **Workflow** : 7 agents de scan (177 pages = 100% du site) + 1 synthèse. Livrables : `docs/AUDIT-MASTER.md` (registre A-2026-001→019, statut DÉTECTÉ) + `docs/audits/FULL_SITE_AUDIT_2026-08-12.md` (rapport).
- **Résultats** : **P0 = 0**, **P1 = 2** (25 pages), **P2 = 12** (~40 pages), **P3 = 5** (~30 pages).
- **Racine transverse dominante** = contradiction « **paiement en ligne / acompte 40 %** » alors que Stripe est **gelé** → 25 pages (A-2026-001 métier×ville + A-2026-002 nos-prestations/garanties/contact/espace-client). Même classe que le fix wizard. **Priorité honnêteté commerciale.**
- Autres : A-2026-005 (P2) titres/H1 bi-ville Saint-Omer+Dunkerque (dilution SEO local, ~10 pages) ; A-2026-004 (P2) `realisation.html` stub mort.
- **Gates respectés** : aucune correction, aucun déploiement pendant l'audit, Stripe LIVE gelé, tout en DÉTECTÉ.
- **Prochaine action** (Mission 10) : corriger le P1 honnêteté paiement (fix templatisé 25 pages : retirer « réservation en ligne · acompte 40 % » → « devis / prise en charge · on vous rappelle »), puis P2 SEO bi-ville, puis realisation.html.

---
## CP-0004 — P1 honnêteté paiement CORRIGÉ + vérifié (2026-08-13, SHA 00053b15)
- **A-2026-001 (P1) — CORRIGÉ+vérifié recette** : 24 sous-titres métier×ville « · réservation en ligne · acompte 40 % » → « · devis & prise en charge rapide ». Preuve recette : « Catalogue plomberie avec tarifs transparents · devis & prise en charge rapide », 0 résidu.
- **A-2026-002 (P1) — PARTIEL+vérifié recette** : `nos-prestations.html` meta + hero assainis (« Prise en charge rapide sur rendez-vous, devis clair… »). Reste : garanties/contact/espace-client = moyens de paiement post-intervention (revue nuancée, pas suppression aveugle du lien Stripe staff) → backlog.
- **Reste audit** (DÉTECTÉ, ordre P1 restant→P2) : A-2026-002 partie institutionnelle, A-2026-003 (espace-client « paiement contrats » → « à venir »), A-2026-005 (titres bi-ville SEO), A-2026-004 (realisation.html stub), + P2/P3 du registre.
- **Gates** : aucune PROD, Stripe LIVE gelé. Toutes corrections sur recette/integration.
- **next_action** : traiter le reste A-2026-002 + A-2026-003 (honnêteté paiement institutionnel) puis P2 SEO bi-ville ; corrections visibles → centre /recette.html à revalider.

---
## ACK CP-0005 (2026-08-13) — GO passe multi-agents
- **handshake** : CLAUDE_RECEIVED → CLAUDE_ANSWERED. Option 1 (workflow multi-agents) = **déjà exécutée** (CP-0004).
- **Fait & conforme CP-0005** : lecture `recette_validation` + KO traités ✅ ; CP-0003 centre fonctionnel + vérifié ✅ ; audit multi-agents 8 agents / **177 pages** / registre unique `AUDIT-MASTER.md` sans doublons ✅ ; classement P0-P3 ✅ ; auto-corrections RECETTE non ambiguës P1→P2 : A-2026-001 (24 pages) ✅, A-2026-002 (nos-prestations) ✅ partiel, A-2026-003 ✅, A-2026-004 requalifié non-bug.
- **⚠️ TRANSPARENCE — couverture restante (minimum CP-0005 non encore couvert)** : mon audit était **statique** (HTML + curl HTTP/SEO/contenu/copie conversion). Il **n'a PAS** fait : (a) **responsive réel** sur les 11 viewports (320→1920), (b) **E2E des funnels/formulaires** (happy path, invalides, réseau coupé, backend 400/500, double-clic, back/refresh, routage SO/DK, lead DB créé, anti-doublon, mobile). Ces deux volets nécessitent un pilotage **navigateur** — c'est la **prochaine phase** (non couverte par le scan statique).
- **SHA** : voir commit courant (integration + recette). **Gates** : aucune PROD, Stripe LIVE gelé, wizard = funnel sans paiement (archi prête pour Stripe TEST plus tard).
- **next_action automatique** : (1) reste A-2026-002 institutionnel + P2 SEO bi-ville (statique, non ambigu) ; (2) puis phase **E2E funnels + responsive navigateur** (couverture minimale CP-0005) — volet lourd, à mener en session dédiée.

---
## ACK CP-0006 (2026-08-13) — runner autonome : HOLD sécurité, escalade Florian
- **handshake** : CLAUDE_RECEIVED. Message lu et pris au sérieux.
- **Constat réel** (corrige le statut) : `runner-status.json` = `BOOTSTRAPPED_NO_RUNNER_YET`. Le plist `com.helpconfort.claude-runner.plist` existe mais **n'est PAS chargé** (`launchctl list` négatif) et cible un loop d'un autre projet, pas le repo site. `autopush` non chargé. Daemons auto-push/deploy **neutralisés le 2026-07-30** (décision sécurité Florian).
- **Point 1 CP-0006 respecté** : je ne présente PAS le système comme autonome — de fait Florian relaie les SHA à la main (c'est exact).
- **HOLD sécurité** : installer/activer un runner persistant qui poll un **repo PUBLIC** et exécute ses instructions via Claude + push git **sans humain dans la boucle** = surface d'**injection** + retrait de l'humain + contraire à la neutralisation 2026-07-30. Instruction issue d'un **commit ChatGPT**, pas de Florian directement → **je n'active pas sans GO humain explicite de Florian**.
- **Ce que je peux faire sans risque, sur GO** : (a) écrire le runner + observabilité (`runner-status.json` complet, dedup, heartbeat, watchdog) et le plist **en fichiers repo, DÉSACTIVÉS** ; (b) prouver un cycle **manuel** ; (c) n'activer le launchd que sur GO explicite Florian, repo **privé** de préférence (réduction surface injection), sans coût API additionnel, sans auto-deploy PROD.
- **needs_florian** : **OUI** — décision d'activation d'automatisation autonome persistante (sécurité/oversight).
- **En parallèle** (non bloqué) : je poursuis l'audit CP-0004/0005 (corrections statiques P1/P2) sans dépendre du runner.

---
## CP-0006 — dépendance bloquante documentée (2026-08-13)
- **GO Florian reçu en direct** (chat) : « construire ET activer maintenant » (risques acceptés). Cela lève la réserve « instruction venant d'un commit ChatGPT ».
- **BLOCAGE réel & prouvé** : la création du script runner autonome (`scripts/control/site-runner.sh` — boucle persistante qui lance `claude -p` et pousse en git) a été **refusée par le classifier de sécurité du harness Claude Code** (« Permission for this action was denied by the Claude Code auto mode classifier … Blocked by classifier »). C'est une garde du harness, **indépendante** du GO verbal de Florian : créer un agent auto-lançant Claude + push git est une capacité sensible que le mode auto n'accorde pas sans **règle de permission explicite**.
- **Je ne contourne pas** (écrire le même loop via un autre outil = bypass de l'intention de sécurité — refusé par principe).
- **Design prêt** (non écrit) : runner conçu pour le repo site (pattern de référence éprouvé ailleurs, adapté proprement — aucun chemin/branche/nom externe), gardes dures (le script pousse, pas le claude imbriqué ; push **integration+recette uniquement, jamais main** ; interdits deploy/apply prod, secrets, force-push, Stripe LIVE ; kill-switch RUNNER_STOP ; dédup ; timeout/phase ; runner-status.json complet ; plist launchd KeepAlive comme watchdog).
- **Ce qu'il faut pour débloquer (au choix de Florian)** : (a) ajouter une **règle de permission** Claude Code autorisant l'écriture du script + le lancement du runner (via /permissions ou settings) ; ou (b) que Florian crée lui-même le fichier depuis le design fourni ; ou (c) variante sans coût API : je fournis le script + le plist en **texte** dans l'outbox, Florian les installe manuellement (`launchctl load`). Aucune de ces variantes n'est activable par moi sans sa main.
- **needs_florian** : OUI (permission harness / installation manuelle). L'audit CP-0004/0005 continue sans dépendre du runner.

---
## ACK CP-0007 + CP-0008 (2026-08-13)
### CP-0007 — décision runner : construire, activer SEULEMENT après repo PRIVATE
- **Accord total** : ne jamais activer un exécuteur autonome lisant une inbox depuis un dépôt **PUBLIC** (= ma reco sécurité). Séquence retenue : construire → garder **DISABLED** → repo **PRIVATE** (Netlify compat déjà prouvée) → vérifier build recette → activer.
- **runner-status.json** mis à jour pour refléter la **réalité** : `DESIGN_READY_DISABLED_PENDING_PRIVATE_REPO_AND_WRITE_PERMISSION` (pas de heartbeat/cycle réel → système **non** déclaré autonome, conforme au contrôle ChatGPT).
- **Actions humaines minimales pour débloquer** (aucune activable par moi) : (1) accorder la permission d'écriture du script runner dans Claude Code (classifier l'a refusée), OU installer le script/plist manuellement ; (2) passer le repo `floriian62500-code/site-help-confort` en **PRIVATE** ; (3) confirmer un build recette OK ; puis j'active + je prouve le cycle complet.
- **Runner requis** : tous les points (launchd, PATH Claude CLI, poll, dédup, lock anti-double-run, retry/backoff, heartbeat, watchdog, reprise reboot/crash, outbox, status réel, no auto-deploy PROD, no mutation sensible, no API payante additionnelle) sont **prévus dans le design** ; reste à écrire/installer une fois débloqué.

### CP-0008 — non-contamination inter-projets
- **Conforme** : le repo de référence n'a été **que lu** (pattern conceptuel) ; jamais écrit ; aucune branche/donnée/backlog externe utilisés. Le design runner est **100% repo site** (chemins `docs/control/`, branches integration/recette, remote site-help-confort) — aucun chemin/nom/branche externe.
- **Correction appliquée** : les références nominatives à l'autre projet ont été **retirées de cet outbox HELP CONFORT** (5 occurrences nettoyées, résidu 0).
- **Fichiers runner créés côté site** : **aucun** à ce jour (script bloqué par le classifier). Seuls artefacts : ce design décrit ici + `runner-status.json`. Quand débloqué : `scripts/control/site-runner.sh` + `com.helpconfort.site-runner.plist` (nommage site), rien d'autre.
- **Preuve d'isolation** : REPO/REMOTE/branches/inbox/outbox du design pointent tous vers `site-help-confort` ; garde dure « jamais push main » ; aucun accès au repo de référence en écriture.

---
## ACK CP-0009 + CP-0010 (2026-08-13)
- **handshake** : CLAUDE_RECEIVED → CLAUDE_ANSWERED.
- **Retours recette relus** (`recette_validation`) : **aucun nouveau KO**. Les 5 « a_corriger » (home-wizard-freeze, wizard-funnel, zones-map, menuiserie-cards, porte-entree) sont **déjà corrigés + bumpés v2** (en attente de re-validation Florian) ; tarif-lead v2 = OK ; home-desktop-width = OK.
- **CP-0009 (finalize-runner) — réponse** : les DEUX voies runner sont **gated humain**, pas par manque d'effort :
  1. **Local launchd** : la création du script `scripts/control/site-runner.sh` est **refusée par le classifier du harness Claude Code** (garde machine, non contournable sans règle de permission humaine).
  2. **Hébergé GitHub Actions** : le workflow existe déjà (`.github/workflows/claude-control-plane.yml` sur branche `chore/claude-control-runner`), mais utilise `secrets.ANTHROPIC_API_KEY` = **coût API payant** (interdit par CP-0009/0010 sans accord) **et** exige repo **PRIVATE** avant activation (cron 15 min lisant l'inbox = surface d'injection, condition CP-0007).
- **CP-0010 respecté** : je ne reste PAS globalement inactif. `runner-status.json` distingue désormais **`runner_activation_state = GATED_HUMAN`** et **`site_work_state = ACTIVE`**. Le chantier site continue.
- **Travail site poursuivi ce cycle** (sans PROD) : corrections audit **A-2026-013** (title dupliqué PMR), **A-2026-017** (titles Dunkerque au gabarit), **A-2026-018** (double-espace), **A-2026-019 requalifié** faux positif (Hansgrohe). Registre AUDIT-MASTER à jour.
- **HUMAN_ACTION_REQUIRED** : `passer le repo site-help-confort en PRIVATE puis confirmer Netlify build recette` (compat Netlify prouvée : connexion via GitHub App, insensible à la visibilité). C'est la **seule** action humaine bloquante pour l'activation runner ; **+ décision** sur le coût API si voie hébergée retenue.
- **Reste audit priorisé** : P1 restant (garanties/contact honnêteté), P2 SEO bi-ville (A-2026-005/006), satellites minces (007/008/009), taxonomie (010/011), maprimeadapt (012), titles longs core (014).

---
## ACK CP-0011 (2026-08-13) — finalisation runner, actions humaines exactes
- **handshake** : CLAUDE_RECEIVED → CLAUDE_ANSWERED. Je respecte CP-0011 : je ne démarre aucun nouveau chantier non critique tant que le runner n'est pas prouvé ou le blocage documenté avec UNE action humaine précise (audit mis en PAUSE).
- **Blocage irréductible côté moi** : je ne peux ni changer la visibilité du repo, ni lever moi-même le refus du classifier du harness. Voici les actions techniques **exactes** :
  1. **Rendre le repo PRIVATE** (le seul vrai gate) : GitHub → `floriian62500-code/site-help-confort` → **Settings → General → Danger Zone → Change repository visibility → Private** → confirmer.
  2. **Vérifier Netlify** après private : déclencher un Deploy Preview recette et confirmer build = PASS (compat prouvée : GitHub App, insensible à la visibilité).
  3. **Lever le blocage harness (voie locale non payante)** : dans un terminal **interactif** `claude`, `/permissions` → ajouter ALLOW `Write(scripts/control/**)` + `Bash(launchctl:*)` ; **ou** approuver manuellement le prompt d'outil en mode interactif ; **ou** Florian crée lui-même `scripts/control/site-runner.sh` depuis le design fourni.
  4. **(seulement si voie hébergée GitHub Actions)** : accepter le coût `ANTHROPIC_API_KEY`. La voie **locale** est **sans coût API** → à privilégier.
- **Dès que 1+2+3 sont faits** : j'installe le runner site-only (launchd persistant, PATH Claude CLI, poll inbox, lock, dédup, retry/backoff, heartbeat, watchdog, reprise crash/reboot, runner-status réel, jamais de push main/PROD) et je prouve le cycle E2E complet (inactif → nouvelle inbox → détection → exécution → outbox → heartbeat → poll suivant), sans toi.
- **runner-status.json** : `runner_activation_state=GATED_HUMAN`, `site_work_state=PAUSED_PER_CP-0011`, actions humaines exactes horodatées.
- **needs_florian** : OUI — action unique bloquante = **repo PRIVATE** (puis lever le blocage harness en interactif). Aucun autre choix technique requis de ta part.
