# RUN Claude — 2026-08-28T09:05Z — P0 callback 'Être rappelé' réparé (issue #9 / 5450259719)

run_id: run-20260828-0905-callback

- ACK : CLAUDE_ANSWERED — in_reply_to 5450259719 (P0 fonctionnel callback).
- CAUSE RACINE : le front envoyait `form_type='home_callback'`, **inconnu** du contrat de l'edge `submit-lead-v6`. Contrat par défaut = `demande_metier` (l.97) qui **exige `code_postal` + `ville`** → HTTP 400 « Code postal requis / Ville requise » (le callback ne collecte pas d'adresse) → message rouge.
- FIX (front, à la source, `d099417b`) : `form_type='rappel'` — contrat existant (l.95) qui n'exige que **nom/prénom + un contact (tél OU email)**. Aucune modif serveur (pas de deploy edge). Fichier : `index.html`.
- PREUVE BACKEND : POST réel → **HTTP 200**, `{"success":true,"id":"91304573-…","contract_v6":true}` (lead créé côté serveur ; test « NE PAS TRAITER »). L'ancien payload `home_callback` → 400 reproduit.
- UX durcie (point 4) : succès honnête « Un conseiller de votre agence locale vous rappelle rapidement » (pas de délai garanti fictif) ; échec = message non technique + **champs conservés** + bouton **« Réessayer »** + téléphone en secours ; anti-double-submit conservé ; aucun faux succès.
- TESTS (navigateur) : (1) succès nominal → message vert + reset ✓ ; (2) erreur réseau simulée → message rouge, champs conservés, bouton « Réessayer », tél visible ✓ ; (3) double-clic → 2e clic bloqué (lock `dataset.sending`) ✓.
- SÉCURITÉ : validation format tél côté edge conservée ; honeypot + rate-limit edge conservés ; aucune donnée sensible en console ; aucun secret front.
- LEADS DE TEST créés (2) : prénom « NE PAS TRAITER » + message explicite → à ignorer par l'agence.
- LEDGER : CBK-1 **READY_FOR_QA** (`d099417b`). ledger-check 27/27 OK.
- GATES : recette only ; no main/PROD ; no secret ; no force-push ; Stripe LIVE gelé.
- needs_florian : true (validation) ; handshake_status : CLAUDE_ANSWERED.
- RETOUR : `CALLBACK=FIXED | ROOT_CAUSE=form_type home_callback→default contract exige cp/ville→400 | FIX=form_type:rappel | HTTP=200 | BACKEND_RECEIPT=lead id (test NE PAS TRAITER) | TESTS=success+neterr+dblclick PASS | SHA=d099417b | STATUS=READY_FOR_QA`
