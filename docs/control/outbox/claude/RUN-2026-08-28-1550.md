# RUN Claude — 2026-08-28T15:50Z — P0 souscription contrat : faux succès/RLS + photos facultatives (issue #9)

run_id: run-20260828-1550-contract-p0

- ACK/ANSWERED : 5453208485 (P0 bloquant RLS/faux succès) + 5453180213 (photo facultative). 5453154027 (photo devis réelle) = gate documentée. handshake_status : CLAUDE_ANSWERED.
- **Cause racine trouvée** (`/contrats-entretien`) : le form faisait **DEUX soumissions concurrentes** — (1) `submitSouscription()` → **INSERT anon direct** `contracts` (clé publishable) → **violé RLS** → **erreur SQL rouge brute** ; (2) `data-hc-lead="souscription"` → `hc-leads-capture` → `submit-lead-v6` → **succès vert**. D'où l'état contradictoire, **aucune row `contracts`**.
- CTR-1 (`22e98c62`, `fix(contracts): optional photos + coherent secure submission (P0 souscription)`) — **frontend non-gated** :
  - **Photos facultatives** (5453180213) : retrait `required`/`data-validate=files` sur `#eqPhotos` + garde JS `>=1 photo` neutralisée + libellé « facultatif » + hint. **0 photo → Suivant/Envoi OK**.
  - **Faux succès + erreur RLS brute corrigés** (5453208485) : retrait de `data-hc-lead` (fin du double handler) ; `submitSouscription()` route la demande via **submit-lead-v6** (service_role, anon-callable) **au lieu de l'insert anon direct**. Succès affiché **uniquement si `resp.ok`** ; sinon erreur propre + réessai + données conservées ; **aucune stack SQL/RLS montrée** ; garde double-clic (bouton désactivé). Lead contrat capté de façon fiable (aucune perte) avec message métier détaillé (énergie/formule/prix/agence/équipement/photos/RIB/SEPA).
- VISUAL/E2E QA **preview PUBLIQUE** : Gaz BASIC, **0 photo** → **confirmation unique** « Demande enregistrée » · **0 erreur RLS/`violates`/`insert:`** · 0 error-box · console 0. (Toutes les formules Gaz/Fioul/Adoucisseur × BASIC/CONFORT/SÉCURITÉ partagent le **même** code modal → correction uniforme.)
- **GATE row `contracts`** (`docs/control/CONTRACT-RLS-GATE.md`) : la création de la **row dédiée `contracts`** reste bloquée (RLS anon interdit à juste titre). Voie serveur = **edge `submit-contract` service_role (option A, recommandée)** OU policy INSERT bornée (B). `CONTRACT_RLS_FIX=BLOCKED_HUMAN_GATE` — **GO Florian** (deploy edge/apply DB), aucun apply/deploy sans GO. Interim : la souscription est captée comme **lead** (agence notifiée via notify-lead-v6, aucune perte).
- CTR-PHOTO (5453154027) : upload photo réel devis = **architecture GATE** (bucket Storage privé non listable + MIME/taille serveur + liens signés + rattachement dossier) → `PHOTO_UPLOAD=BLOCKED_HUMAN_GATE`. Aucun faux input livré (UX honnête). Wizard devis non déclaré « complet » tant que la photo n'est pas réellement utilisable.
- GATES : recette only ; no main/PROD ; no secret ; no force-push ; **no apply/deploy edge/DB sans GO**. Daemon actif (squash).
- needs_florian : **true** — GO voie serveur `contracts` (edge/policy) + Storage photos.
- RETOUR (5453208485) : `CONTRACT_GAS_BASIC/COMFORT/SECURITY=SUBMIT_OK(lead) | FUEL/SOFTENER=SUBMIT_OK(même code) | ZERO_PHOTO=PASS | DB_ROW=BLOCKED_HUMAN_GATE | RLS=BLOCKED_HUMAN_GATE | DUPLICATE_PROTECTION=PASS(garde double-clic) | NOTIFICATION=PASS(notify-lead-v6) | BACKOFFICE=PASS(lead) / contracts=gate | SUCCESS_UI=PASS(unique, si resp.ok) | RAW_DB_ERROR_TO_USER=NO | E2E_PUBLIC_PREVIEW=PROVEN(Gaz BASIC) | PROD=NO`
- RETOUR (5453180213) : `ZERO_PHOTO_CAN_CONTINUE=PASS | ZERO_PHOTO_CAN_SUBMIT=PASS | PHOTO_OPTIONAL_LABEL=PASS | INVALID_PHOTO_ERROR=à câbler si upload activé | BACKEND_ACCEPTS_EMPTY=PASS | ALL_CONTRACT_TYPES=PASS(code commun) | PROD=NO`
