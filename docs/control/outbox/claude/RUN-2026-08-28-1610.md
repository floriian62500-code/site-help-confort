# RUN Claude — 2026-08-28T16:10Z — Cartes réalisations cliquables (issue #9)

run_id: run-20260828-1610-realisations-clickable

- ACK/ANSWERED : 5453363515 (cartes réalisations non cliquables). handshake_status : CLAUDE_ANSWERED.
- REAL-CLICK (`f8e5d39f`, `fix(realizations): make project cards fully clickable to real slug`) :
  - Cause : le **fallback edge** `realisations-json` ne renvoie pas de champ `url` → `href="#"` (cartes mortes) ; le chemin Supabase pointait `/realisations/<slug>` sans `.html`.
  - Fix : **normalisation de l'url de CHAQUE carte** (2 sources) → `realisations/<slug>.html` ; jamais de `#`. Carte déjà `<a>` englobante (surface entière) + `cursor:pointer` + `:focus-visible` (clavier). 28 hrefs **distincts** (slug réel). Home showcase déjà `<a>` full-card OK.
- QA **preview PUBLIQUE** : 28 cartes, 0 `href="#"`, hrefs distincts = slug réel ; navigation vers page détail réelle OK.
- **Caveat honnête (gap contenu, pas 404)** : **25/28** slugs edge ont une page détail statique → navigation correcte ; **3/28** slugs edge n'ont PAS de page statique (`nouvelle-realisation-signee-help-confort-saint-omer`, `nouvelle-renovation-realisee-par-help-confort-saint-omer`, `remplacement-de-la-serrurerie-sur-porte-dentree`) → **redirigés vers le listing** (soft fallback via `_redirects`, pas d'erreur). = **gap de pipeline contenu** : générer les 3 pages manquantes OU l'edge ne renvoie que les slugs avec page. Flag séparé.
- RETOUR : `FULL_CARD_CLICK=PASS | CORRECT_SLUG=PASS(25/28) | KEYBOARD=PASS(focus-visible) | HOVER_FOCUS=PASS | CAROUSEL_CONTROLS=NO_REGRESSION | HOME_CARDS=PASS(déjà <a>) | CONTENT_GAP=3_MISSING_DETAIL_PAGES(flag) | PROD=NO`
