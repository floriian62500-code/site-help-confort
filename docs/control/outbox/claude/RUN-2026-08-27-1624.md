# RUN Claude — 2026-08-27T16:24:12Z — CMD-5 analyse réf : précondition « 0-réf » FAUSSE (issue #9)

run_id: run-20260827-162412-9d90fb6d

- TASKS_ANALYZED_THIS_RUN : CMD-5 (retrait ancien sélecteur) — 1re étape implém UX-COMMERCE-1.
- FINDING (honnêteté, pas de suppression risquée) : l'ancien sélecteur **n'est PAS inerte**. `buildResult()` branche `simple` (index.html l.2235-2293) exécute encore `detectDetailedPresta()` (l.2237) et `renderPrestaProposals()` (l.2273/2281/2291) ; sortie vers stubs **cachés** (l.1254) tandis que le catalogue/panier est ce qui est visible. `resa-presta-name-c` (l.2226/2295) sert aussi le récap confirmation.
- CONSÉQUENCE : suppression aveugle des 3 fonctions → `ReferenceError` à l.2237/2273 → **wizard cassé** sur le flux prestation `simple`. La précondition écrite « preuve 0-réf → suppression » est donc **fausse en l'état**.
- DÉCISION : **ne pas supprimer maintenant** (risque de casse d'un parcours de réservation live recette). Reséquencé : **PRÉREQUIS = UX-C-e** = migrer la branche `simple` de buildResult + le récap confirmation sur le moteur panier `hc-cart.js`, PUIS supprimer les symboles morts. Ledger CMD-5 corrigé en conséquence (BACKLOG — bloqué par preuve 0-réf fausse, prérequis UX-C-e).
- CHANGES : `docs/control/MASTER-TASK-LEDGER.md` (ligne CMD-5 corrigée) — SHA `9d90fb6d`. Aucun code applicatif modifié ce run (analyse read-only + correction ledger).
- REFUSED_OR_NOT_DONE : suppression CMD-5 refusée ce cycle (précondition non satisfaite, risque de casse) — reséquencée proprement, pas abandonnée (règle non-disparition).
- GATES : recette only ; no main/PROD ; no secret ; Stripe LIVE gelé ; no force-push.
- NEXT_TASK : UX-C-e (migration buildResult.simple + récap → panier) qui débloque CMD-5 ; en parallèle UX-C-a (sous-familles + fiche prestation enrichie).
- needs_florian : true (décision de séquencement UX + risque de casse wizard signalé) ; handshake_status : CLAUDE_ANSWERED.
