# Fichiers à supprimer manuellement

Ces fichiers ne sont **plus référencés** dans aucune page HTML, mais le sandbox Claude
ne peut pas les supprimer directement (permissions).

**Action requise** : supprimer manuellement via Finder ou terminal :
```bash
rm "_to_delete_mascotte.tmp.png"
rm "_to_delete_mascotte-opt.tmp.png"
rm "_to_delete_mascotte-with-bg.png"
rm "_to_delete_mascotte1.png"
```

**Économie** : ~2,2 Mo

Date : 28 mai 2026
